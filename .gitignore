# Ignore the git and zip files we use
.*
*.zip
x_samconfig*.toml

# Ignore program and OS files
.vscode
*.DS_Store
*desktop.ini

# If you need to remove .DS_Store
# find . -name .DS_Store -print0 | xargs -0 git rm -f --ignore-unmatch

# Ignore 
node_modules/
scripts/logs/*

# ----------------------------------------
# UPDATE FOR PRODUCTION USE!
# Comment out these lines when saving generated files to a private repository!

scripts/settings/defaults.json
scripts/settings/*-defaults.json

scripts/settings/service-role/*
scripts/settings/storage/*
scripts/settings/pipeline/*
scripts/settings/network/*

service-role-infrastructure/samconfig-*
storage-infrastructure/samconfig-*
pipeline-infrastructure/samconfig-*
network-infrastructure/samconfig-*

imports/*

# ----------------------------------------

!sample-*
!sample.*
!README.md
!.gitignore
